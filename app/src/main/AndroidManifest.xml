<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.privateapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="גיוס בקליק"
        android:gravity="center"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".FinishProccess"
            ></activity>
        <activity android:name=".Logistica"
            />
        <activity android:name=".Summons"
            />
        <activity android:name=".Attending"
            />
        <activity android:name=".PrivateProfileEntery"
            />
        <activity android:name=".PrivateProfile"
            />
        <activity android:name=".ChooseElements"
            />
        <activity android:name=".OpeningScreen"
            />
        <activity android:name=".MainActivity"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>